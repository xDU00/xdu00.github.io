# Art gallery images

Drop your artwork files here (`png`, `jpg`, `jpeg`, `gif`, `webp`, `avif`).
They are picked up automatically and displayed on the `/art/` page.

The filename becomes the caption — use dashes or underscores for spaces.
For example: `red-sunset.jpg` → caption "red sunset".
